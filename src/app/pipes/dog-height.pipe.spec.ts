import { DogHeightPipe } from './dog-height.pipe';

describe('DogHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new DogHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
